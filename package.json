{
  "name": "justscan",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "description": "JustScan — A Portable CamScanner. No fuss, just scan.",
  "author": "Joe Dacillo",
  "scripts": {
    "dev": "npm run copy-opencv && vite --host",
    "build": "npm run copy-opencv && vite build",
    "preview": "vite preview --host",
    "copy-opencv": "node scripts/copy-opencv.js",
    "postinstall": "node scripts/copy-opencv.js"
  },
  "dependencies": {
    "@techstark/opencv-js": "4.10.0-release.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.10"
  }
}
